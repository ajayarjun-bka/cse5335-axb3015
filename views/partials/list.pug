//
   Created by Ajay Arjun on 11/23/2016.
#div
    //button.button-style(ng-click='appDetails()') Click To Start
    #an-fiv
    ul(ng-repeat='item in items')
        li(id='list') {{item.pk}} {{item.AGE}} {{item.SEX}} {{item.NATIONAL_PROVIDER_ID}} {{item.LENGTH_OF_STAY}} {{item.DRG_PRICE}}

